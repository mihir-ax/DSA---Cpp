#include <bits/stdc++.h>
using namespace std;

int main()
{

    // // cout << sizeof(char);

    // int x = 10;

    // cout << &x << "\n";

    // // store addresses in a pointer !

    // int *p = &x;
    // cout << p;

    // INCREAMENT IN POINTERS !!

    char x = 4;
    char *p = &x;

    cout << p << endl;
    p = p + 1;
    cout << p << endl;

    return 0;
}
